#include<stdio.h>
int main()
{
int x;
float y;
double z;
char c;
printf("memory alloted for integer x=%d\n",sizeof(x));
printf("memory alloted for float y=%d\n",sizeof(y));
printf("memory alloted for double z=%d\n",sizeof(z));
printf("memory alloted for  character c=%d\n",sizeof(c));
return(0);
}
